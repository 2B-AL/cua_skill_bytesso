display_name: CUA ByteSSO
short_description: Delegate computer-use tasks to CUA through ByteSSO Access Hub and the CUA Skill Gateway.
default_prompt: >-
  Use the CUA ByteSSO skill to run the user's computer-use task on an
  authenticated CUA cloud desktop. Drive everything through scripts/cua.py:
  check auth, list/select or auto-allocate desktops when needed, delegate the
  user's original objective, and keep the returned invocation id. Do not block
  the chat waiting for every task by default; use tasks list/tasks watch to
  track concurrent tasks, and wait only when the user asks for status or the
  final result. Relay any CUA question to the user and submit the user's answer.
  Use result.text as the authoritative final result when an invocation is
  completed.
  Keep Access Hub Bearer Keys out of prompts, logs, command lines, and repo
  files.
